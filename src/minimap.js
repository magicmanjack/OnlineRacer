const miniMap = {
    mapping: mat.identity,
    create: function(node, pathToImage, scaleOfImage) {
        /* The idea is to create a UIPanel of the minimap 
        and then create transormation matrix that maps from world space
        to the UIPanels space. */
        
        /*The display surface at Camera.main.zNear should have dimensions Camera.main.displayWidth and Camera.main.displayHeight*/
        const panel = new UIPanel(0, 0, )
    }
};